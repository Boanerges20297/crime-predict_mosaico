services:
  crime-predict:
    build: .
    container_name: crime-predict
    restart: always
    ports:
      - "8091:5000"
    environment:
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./data:/app/data